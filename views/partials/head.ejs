<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> <%= title %> </title>
    <style>
        /* Global Styles */
        body {
        font-family: 'Arial', sans-serif;
        background-color: #1a1a1a; /* Black background */
        color: #dcdcdc; /* Light grey text for contrast */
        margin: 0;
        padding: 0;
        line-height: 1.6;
        }
        form {
            background-color: #262626; /* Dark grey form background */
            padding: 20px;
            border-radius: 10px;
            max-width: 500px;
            margin: 0 auto;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
        }

        a{
            text-decoration: none;
        }
        label {
            color: #dcdcdc; /* Light grey for labels */
            display: block;
            margin-bottom: 10px;
            font-size: 1.2rem;
        }

        input[type="text"],
        textarea {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #800080; /* Dark purple border */
            border-radius: 5px;
            background-color: #333333; /* Darker grey input background */
            color: #dcdcdc; /* Light grey text */
            font-size: 1rem;
        }

        textarea {
            height: 150px;
        }

        input[type="text"]:focus,
        textarea:focus {
            border-color: #e60000; /* Bright red border on focus */
            outline: none;
            box-shadow: 0 0 5px #e60000;
        }

        button {
            background-color: #e60000; /* Bright red button */
            color: #dcdcdc; /* Light grey text */
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            font-size: 1.2rem;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #b30000; /* Slightly darker red on hover */
        }

        h1 {
        color: #e60000; /* Bright red for the main heading */
        text-align: center;
        padding: 20px 0;
        font-size: 3rem;
        }

        h2 {
        color: #b30000; /* Slightly darker red for subheadings */
        border-bottom: 2px solid #800080; /* Dark purple underline */ padding-bottom: 10px;
        margin-bottom: 20px;
        font-size: 2rem;
        }

        p {
        color: #dcdcdc; /* Light grey text */
        font-size: 1.2rem;
        margin-bottom: 20px;
        }

        /* Navigation Styles */
        nav {
        background-color: #800080; /* Dark purple background */
        padding: 10px;
        }

        nav ul {
        list-style: none;
        padding: 0;
        display: flex;
        justify-content: center;
        }

        .links a{
            text-decoration: none;
            color: white;
            font-weight: bold;
        }

        .links a:hover{
            text-decoration: underline;
            color: rgb(175, 19, 175);
            font-style: italic;
        }
        nav ul li {
        margin: 0 20px;
        }

        nav ul li a {
        color: #dcdcdc; /* Light grey links */
        text-decoration: none;
        font-size: 1.2rem;
        transition: color 0.3s ease;
        }

        nav ul li a:hover {
        color: #e60000; /* Bright red on hover */
        }

        /* Blog Post Styles */
        .blog-post {
        background-color: #262626; /* Darker grey for blog post container */
        padding: 20px;
        border-radius: 10px;
        margin-bottom: 20px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
        }

        .blog-post h2 {
        color: #e60000; /* Bright red for blog titles */
        }

        .blog-post p {
        color: #b3b3b3; /* Slightly darker grey for blog snippets */
        }

        /* Footer Styles */
        footer {
        background-color: #800080; /* Dark purple background */
        color: #dcdcdc; /* Light grey text */
        text-align: center;
        padding: 20px 0;
        }

        footer p {
        margin: 0;
        font-size: 1rem;
        }

    </style>
</head>